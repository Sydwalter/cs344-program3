class name: CS344
student name: Scott Milton
program name: smallsh

To compile the program, navigate into the directory that has the files
smallsh.c and the Makefile. Next, simply type make from the command line.
You can then run the program by typing the command ./smallsh  
